{
  "id": "bench/local/tier3-task-probe",
  "level": "L3",
  "track": "both",
  "code": "def run():\n    scale = globals().get('BRIDGE_SCALE', 1.0)\n    globals()['TASK_METRICS'] = {'loss_curve': [1.0 * scale, 0.5 * scale, 0.25 * scale], 'accuracy': 0.8 / scale}\n    return [1.0, 2.0, 3.0]",
  "expected_ops": ["python:task-metrics"],
  "training_config": {
    "epochs": 3,
    "batch_size": 1,
    "precision": "FP32"
  },
  "ground_truth": {
    "atol": 1e-9,
    "rtol": 1e-9,
    "rationale": "Final output is invariant while task metrics expose Tier-3 drift."
  },
  "difficulty": "intermediate",
  "failure_mode": "training_divergence",
  "seed": 42
}
