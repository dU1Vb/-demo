{
  "id": "bench/local/tier2-activation-probe",
  "level": "L2",
  "track": "both",
  "code": "def run():\n    scale = globals().get('BRIDGE_SCALE', 1.0)\n    hidden = [scale * x for x in [1.0, 2.0, 4.0]]\n    globals()['ACTIVATIONS'] = {'hidden': hidden}\n    return [x / scale for x in hidden]",
  "expected_ops": ["python:activation-probe"],
  "ground_truth": {
    "atol": 1e-9,
    "rtol": 1e-9,
    "rationale": "Final output is invariant to scale, while ACTIVATIONS expose Tier-2 drift."
  },
  "difficulty": "intermediate",
  "failure_mode": "activation_drift",
  "seed": 42
}
