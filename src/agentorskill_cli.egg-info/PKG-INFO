Metadata-Version: 2.4
Name: agentorskill-cli
Version: 0.1.0
Summary: CLI to evaluate deep learning framework migration libraries (e.g. TorchAX, MindTorch)
Requires-Python: >=3.10
Description-Content-Type: text/markdown
Requires-Dist: typer[all]>=0.12.3
Requires-Dist: pydantic>=2.5
Requires-Dist: httpx>=0.27
Requires-Dist: rich>=13.7
Requires-Dist: openai>=1.40
Provides-Extra: dev
Requires-Dist: pytest>=8.0; extra == "dev"
Requires-Dist: ruff>=0.4; extra == "dev"

# agentorskill-cli

Evaluate migration libraries (TorchAX, MindTorch, …) with a reproducible CLI: hardware probe, optional LLM-based README extraction → `AdapterSpec`, then smoke / core / model / numeric / benchmark suites and JSON + Markdown reports.

## Install

```bash
pip install -e .
```

## Usage

```bash
# Show help
eval-migration --help

# Probe hardware (with confirmation)
eval-migration probe

# Run with a hand-written adapter JSON (no LLM)
eval-migration run --adapter-file examples/torchax_adapter.json --suite smoke --yes

# Extract adapter from docs + run (requires OPENAI_API_KEY)
eval-migration run --readme-url https://raw.githubusercontent.com/google/torchax/main/README.md --suite smoke --yes
```

Environment:

- `OPENAI_API_KEY` — optional; required for LLM-based doc extraction unless `--adapter-file` is given.

## Adapter JSON

See `examples/` for minimal `AdapterSpec` samples.
